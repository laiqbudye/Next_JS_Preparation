1. to make next js project 

npx create-next-app hello-world


in package.json it has 3 dependecies by default.

1. next  - it a framework built on top of react which contains all the pkgs and code required to build feature rich app
2. react - to build UI
3. react-dom - to mount react app


project flow

1. npm run dev > it goes to _app.js file where it receives component as its arg > that component arg is coming from index.js

simple words - npm run dev -> _app.js -> index.js

